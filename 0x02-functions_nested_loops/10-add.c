#include "main.h"
/**
 * add - returns the addition of two integers.
 * @x: first int
 * @y: second int
 * Return: returns the add value of the two arguments
 */
int add(int x, int y)
{
	return (x + y);
}
