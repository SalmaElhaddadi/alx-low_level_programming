#include "main.h"
/**
 * _abs - absolute of integer.
 *
 * @n: argument value.
 *
 * Return: returns absolute value.
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
